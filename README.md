# 3-Tier-Web-Application-using-Docker
A simple 3-tier web application built using Flask (backend), HTML (frontend), and PostgreSQL (database), fully containerized using Docker and orchestrated with docker-compose. This project demonstrates container networking, volume management, and multi-service architecture.
